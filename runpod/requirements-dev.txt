# CI/test dependencies only
runpod==1.8.1
Pillow==12.1.1
numpy==1.26.4
opencv-python-headless==4.10.0.84
pytest
pytest-cov
ruff

# Ultralytics without torch (mocked in tests anyway)
ultralytics>=8.0.0

# CPU-only torch for CI — much lighter than full CUDA version
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cpu